[제목]
Custom domain HTTPS certificate stuck at "new"/"authorization_created" for 24+ hours (repo: ookkoo3000-source/teachfit-site, domain: slovrest.com)

[본문]
Repository: ookkoo3000-source/teachfit-site
Custom domain: slovrest.com (apex) and www.slovrest.com

I added slovrest.com as a custom domain for GitHub Pages roughly 24+ hours ago. DNS has been correctly configured the entire time:
- A records for the apex (@) point to 185.199.108.153 / 109.153 / 110.153 / 111.153
- CNAME for www points to ookkoo3000-source.github.io
- No CAA records restricting certificate authorities
- Verified via Google DNS (8.8.8.8) and Cloudflare DNS (1.1.1.1) — both resolve correctly and have for the entire 24+ hour period

The site itself serves correctly over plain HTTP (200 OK). However, the automatic HTTPS certificate for the custom domain has never progressed past the "new" / "authorization_created" state in the Pages API response (GET /repos/ookkoo3000-source/teachfit-site/pages), and https_enforced cannot be enabled.

Things I've already tried (all via the Pages API), none of which changed the outcome:
1. Toggling the custom domain off and back on (PUT cname="" then PUT cname="slovrest.com") — multiple times, both immediately and after 30+ minute waits
2. Fully deleting and recreating the Pages site (DELETE then POST /pages)
3. Temporarily switching the primary custom domain to www.slovrest.com and back to slovrest.com — this reset the certificate state display from "authorization_created" back to "new", but it never progressed further even after 30+ minutes of no further changes
4. Switching build_type from "legacy" (Jekyll) to "workflow" (GitHub Actions deploy via actions/deploy-pages) — deployment succeeds and the site serves fine, but the certificate state is unaffected
5. Leaving the domain completely untouched for 30+ minute stretches (twice) — certificate state never advances on its own

Current state as of this writing:
{"url":"https://api.github.com/repos/ookkoo3000-source/teachfit-site/pages","status":"built","cname":"slovrest.com","custom_404":false,"html_url":"http://slovrest.com/","build_type":"workflow","source":{"branch":"main","path":"/"},"public":true,"protected_domain_state":null,"pending_domain_unverified_at":null,"https_certificate":{"state":"new","description":"This domain was recently added. The certificate request process will begin shortly.","domains":["slovrest.com","www.slovrest.com"]},"https_enforced":false}

Could you please check why the Let's Encrypt authorization/certificate issuance for this domain is not progressing, and manually trigger or unstick it if possible?

Thank you!
